TODO: 你的 system prompt。host 會把 prompt_keys 內的檔案讀進來，
並用 run.render_prompt(key, {KEY: value}) 做 {{KEY}} → value 替換。

LANGUAGE RULE: Respond in the same language as the input.

--- Conversation so far ---
{{CONVERSATION}}
--- End ---
